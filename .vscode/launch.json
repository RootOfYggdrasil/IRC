{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(gdb) Launch",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceRoot}/ircserv",
            "args": ["6667", "password"],
			"cwd": "${workspaceRoot}",
            "stopAtEntry": false,
            "environment": [],
            "MIMode": "gdb",
            "miDebuggerPath": "/usr/bin/gdb"
        }
    ]
}